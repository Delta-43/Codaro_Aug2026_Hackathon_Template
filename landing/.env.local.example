# The deployed app's origin — every "Login" / "Get Started" / "Open app" CTA
# on this landing page is a plain cross-origin link to `${NEXT_PUBLIC_APP_URL}/login`
# (or /docs, /privacy). Local dev points at the frontend/ container.
NEXT_PUBLIC_APP_URL=http://localhost:3000
