"use client";

// Tab 4 — Bookings (Upcoming / Past), detail, reschedule, cancel, review.
// Full lifecycle lands in Phase 7. Phase 2 stub: navigable.

export default function BookingsPage() {
  return (
    <section className="py-6">
      <h1 className="text-xl font-semibold tracking-tight md:sr-only">Bookings</h1>
      <p className="mt-6 rounded-lg border border-dashed border-border p-4 text-sm text-muted-foreground">
        Upcoming &amp; Past bookings, detail, reschedule, cancel, and reviews
        arrive in Phase 7.
      </p>
    </section>
  );
}
