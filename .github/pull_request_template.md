<!-- Base branch must be `develop`, not `main` (GitHub defaults to `main`). -->

Closes #

## What changed

<!-- One or two sentences. -->

- [ ] Base branch is `develop`
- [ ] `python -m pytest test -q` passes locally
- [ ] No hard-coded domain words or magic numbers — both live in `domain.config.json`
